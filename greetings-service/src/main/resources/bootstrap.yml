server:
  port: ${PORT:5555}

spring:
  application:
    name: greetings-service
    
com.equalities:
  greeting: "Hello Spring World!"
                
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
